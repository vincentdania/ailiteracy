import Link from "next/link";
import { Award, BookOpen, LayoutDashboard, LogOut, Sparkles, Users } from "lucide-react";
import { signOut } from "@/lib/auth";

export function AppNav({ role }: { role: "USER" | "ADMIN" }) {
  return <aside className="border-b border-[#dce2dd] bg-white lg:fixed lg:inset-y-0 lg:left-0 lg:w-64 lg:border-b-0 lg:border-r"><div className="container-shell flex h-16 items-center justify-between lg:h-full lg:w-auto lg:flex-col lg:items-stretch lg:p-5"><Link href="/dashboard" className="flex items-center gap-2 font-black"><span className="grid size-9 place-items-center rounded-xl bg-[#123c31] text-[#d9f99d]"><Sparkles size={18} /></span>AI Literacy</Link><nav className="hidden flex-1 gap-2 pt-10 lg:grid lg:content-start">{[[LayoutDashboard,"Dashboard","/dashboard"],[BookOpen,"Challenge","/challenge"],[Users,"Referrals","/referrals"],[Award,"Certificates","/dashboard#certificates"]].map(([Icon,label,href]) => { const ItemIcon=Icon as typeof LayoutDashboard; return <Link key={String(href)} href={String(href)} className="flex items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold text-[#5f6f67] hover:bg-[#eef3e9] hover:text-[#123c31]"><ItemIcon size={18} />{String(label)}</Link>; })}{role === "ADMIN" && <Link href="/admin" className="mt-4 rounded-xl bg-[#123c31] px-4 py-3 text-sm font-bold text-white">Admin control plane</Link>}</nav><form action={async () => { "use server"; await signOut({ redirectTo: "/" }); }} className="hidden lg:block"><button className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-bold text-[#5f6f67] hover:bg-red-50 hover:text-red-700"><LogOut size={18} />Sign out</button></form><div className="flex gap-1 lg:hidden"><Link className="p-2" href="/challenge"><BookOpen size={20} /></Link><Link className="p-2" href="/referrals"><Users size={20} /></Link></div></div></aside>;
}
